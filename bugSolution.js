The solution involves using the optional chaining operator (?.) or a conditional statement to check if the state property exists before trying to access it.  This prevents the app from crashing and handles the case where the data is not yet available.